import java.util.Scanner;

public class Program {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        Circulo c = new Circulo();
        Ponto p = new Ponto();
        System.out.println("Informe o Ponto ! ");
        p.setPonto(sc.nextDouble());
        System.out.println("Informe o Raio ! ");
        c.setRaio(sc.nextDouble());
        System.out.println(p.getPonto());
        System.out.println(c.getRaio());
c.CriarCirculo(5,10);

        sc.close();
    }
}


public class Circulo {
    
    private double raio;
    private Ponto ponto;

    public void setRaio(double raio){
        this.raio=raio;
    }

    public double getRaio(){
        return raio;
    }
public void CriarCirculo(double a , double b){
      Circulo c2 = new Circulo();
    System.out.println(c2);
}

    @Override
    public String toString() {
        return "Circulo{" +
                "raio=" + raio +
                ", ponto=" + ponto +
                '}';
    }
}

public class Ponto {
    private double ponto;
    public void setPonto(double ponto){this.ponto=ponto;}
    public double getPonto(){return ponto;}
}

